# DSH Agent 项目 AGENTS

> 项目命名：DSH Agent。做一个开箱即用的 AI Agent 分发渠道，让零基础用户扫码→注册→下载→双击→对话，零术语、零命令行、零配置。

## 项目结构

- 公开仓库（本仓库）：产品代码、打包、官网下载页、发布。
- 私有资料库（独立私有仓库）：想法、方案、SPEC、复盘、内部讨论，**不进入本仓库**，详见下文「仓库分工与同步契约」。

## 项目约定

- 默认中文，先给结论。
- 引导 Agent 开源（MIT），下载页/市场自运营。
- MVP 先在 Windows 上验证，macOS 后续。
- 所有用户可见文本使用中文。
- 不引入不必要的依赖。
- **对外文本（README、下载页、官网等）禁用"小白"等贬低性、口语化用词**，目标用户统一表述为「用户」「零基础用户」「普通用户」。
- 装机/安装测试一律在 VM 上执行，不在开发机运行安装包或装机流程。

## 技术栈

- 引导 Agent：Node.js v22（仅内置模块）+ 原生 HTML/CSS/JS
- 打包：pkg --sea 单文件 exe（无 7-Zip SFX 两步引导）
- 下载页：静态 HTML（GitHub Pages）
- 测试：Hyper-V 虚拟机（Windows 10 纯净系统）

## 仓库分工与同步契约（2026-09-01）

- 本仓库（公开）= 项目工作区：代码、打包脚本、官网下载页、Release 发布。
- 私有资料库 = 设计仓库：方案、SPEC、复盘、TODO，因含内部讨论与未定稿内容而不公开。
- 同步责任：**agent 每次把私有资料库中的方案落地到本仓库时，必须同步在本仓库 `docs\` 生成或更新对应实施文档，并随代码一起 commit**；每轮会话结束时做一次两仓库对账。
- **未做同步抄送 = 落地任务视为未完成**。人（jack）负责抽查。
- 本地开发机的内部工作规则（路径、端口、沙箱等）见开发机全局 AGENTS，不写入本公开仓库。